// Scheduler logic has been moved to /api/cron/reminders route.
// On Vercel, the cron job is configured in vercel.json and runs on a schedule.
// For local development, you can manually trigger it via GET /api/cron/reminders.
export {};
